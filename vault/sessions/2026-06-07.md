---
tags: [session, tolvi-solo]
date: 2026-06-07
status: active
---

## [16:30] Session — Ship recall/sync/commit slash commands; fix tolvi-sync hook

**Tickets:** none

### What happened
- Added the three generic slash commands (`tolvi-recall`, `tolvi-sync`, `tolvi-commit`) under `commands/` so solo gets agent-side synthesis, not just the enforce-only commit gate — the missing carrot to the hook's stick.
- Wired command installation into `install.sh` (bundled with `--with-hooks`, skip-if-exists so it never clobbers a user's own commands).
- Documented the new commands and what they add in the README; fixed a stray double-space in `hooks/tolvi-sync` (the only real drift from the main `tolvi` hook — the CLI-mention difference is intentional since solo ships no CLI).

### Files touched
- `commands/{tolvi-recall,tolvi-sync,tolvi-commit}.md` — new (identical to tolvi's)
- `install.sh` — `install_commands()` + call under `--with-hooks`
- `README.md` — slash-commands section under "With Claude Code hooks"
- `hooks/tolvi-sync` — removed double-space typo

### Left open
- Uncommitted working-tree changes (includes the pre-existing edit to `vault/sessions/2026-06-05.md`, unrelated to this work) — commit at your own cadence.

See also (other repos same day): [[2026-06-07]] in `tolvi/vault/sessions/`, [[2026-06-07]] in `marketing-site/vault/sessions/`, [[2026-06-07]] in `ta-ai-tooling/vault/sessions/`
