---
tags: [session, tolvi-solo]
date: 2026-06-05
status: active
---

## [15:30] Session — Initial scaffold, vault provisioned, hooks renamed, repo made public

**Tickets:** none

### What happened
- Scaffolded repo from scratch: `install.sh`, Engineer pack (5 decision templates, pattern, session), Claude Code hooks, README, LICENSE, NOTICE, CONTRIBUTING.md. All templates updated with full `tolvi-format-v1` frontmatter (`repo`, `ticket`, `user_impact`, `product_area`, repo-slug tag) to match sync-session skill format.
- Provisioned vault using `./install.sh` (engineer pack); added `.gitkeep` to empty subdirs; committed vault to repo.
- Prepared repo for public release: description, topics, homepage URL set via `gh repo edit`; `main` branch protection wired (PR required, no force-push for non-admins, enforce_admins OFF); repo flipped public via `gh repo edit --visibility public`.
- Added `CLAUDE.md` with vault index block placeholder.
- Renamed hooks: `session-recall.sh` → `tolvi-recall` (SessionStart, same behavior); `commit-sync-nudge.sh` → `tolvi-sync` (changed from PostToolUse nudge to PreToolUse block). `tolvi-sync` now auto-stages vault/ before commit and blocks if no session note exists for today.
- Updated `install.sh` Python hook wiring to use `PreToolUse` for `tolvi-sync`.

### Files touched
- `install.sh` — initial creation + updated hook names/PreToolUse wiring
- `hooks/tolvi-recall` — new (renamed from session-recall.sh, same behavior)
- `hooks/tolvi-sync` — new PreToolUse enforcement hook (replaces commit-sync-nudge.sh)
- `packs/engineer/templates/decision*.md` — all 5 decision templates: added repo, ticket, user_impact, product_area, repo-slug tag
- `packs/engineer/templates/session.md` — added repo-slug tag to frontmatter
- `packs/engineer/templates/pattern.md` — added language-or-framework tag placeholder
- `vault/templates/*` — synced from pack templates
- `vault/.vault-meta.json` — provisioned (workspace: tolvi-solo, pack: engineer)
- `CLAUDE.md` — new: project context + vault index block
- `README.md` — full rewrite from stub
- `NOTICE`, `CONTRIBUTING.md`, `LICENSE` — added for public release
- `hooks/session-recall.sh`, `hooks/commit-sync-nudge.sh` — deleted (replaced)

### Left open
- Writer, Entrepreneur, CPA packs — planned, not started
- `NPM_TOKEN` for tolvi-solo itself not needed yet (no SDK); note for future
- Bootstrap aggregator symlink: run `./scripts/bootstrap.sh --force-symlinks` to wire `tolvi-solo/vault/` into `~/Developer-Vault/` symlink graph

See also (other repos same day): [[2026-06-05]] in `tolvi/vault/sessions/`, [[2026-06-05]] in `marketing-site/vault/sessions/`

See also: [[2026-06-05-tolvi-solo-as-separate-product]], [[2026-06-05-pretooluse-enforcement-for-vault-sync]]

## [18:30] Session — Expand README packs table with Tier 1+2 planned verticals

**Tickets:** none

### What happened
- Added 12 new `planned` packs to the README packs table to match the verticals surfaced on the Tolvi Solo marketing page (`marketing-site` `/solo` grid): Tier 1 = product, designer, consultant, researcher, investor, devops; Tier 2 = data, security, teacher, sales, recruiter, architect. Table is now 16 rows (engineer ✅ + 15 planned), in the same order as the marketing grid, with terse README-style vertical descriptors. `architect` clarified as the building sense to avoid confusion with software architecture.
- Marketing/docs only: no actual pack templates were built; these are forward-looking `planned` entries.

### Files touched
- `README.md` — packs table expanded from 4 to 16 rows

### Left open
- None. Building any of these packs is future work (each is a `packs/<name>/` directory of Markdown templates per CONTRIBUTING.md).

See also (other repos same day): [[2026-06-05]] in `marketing-site/vault/sessions/`
